<style>
.form-edit-box html, .form-edit-box body, .form-edit-box div, 
.form-edit-box span, .form-edit-box applet, .form-edit-box object,
.form-edit-box h1, .form-edit-box h2, .form-edit-box h3, 
.form-edit-box h4, .form-edit-box h5, .form-edit-box h6, 
.form-edit-box p, .form-edit-box blockquote, .form-edit-box pre,
.form-edit-box a, .form-edit-box abbr, .form-edit-box acronym, 
.form-edit-box address, .form-edit-box big, .form-edit-box cite, 
.form-edit-box code, .form-edit-box del, .form-edit-box dfn, .form-edit-box em,
.form-edit-box img, .form-edit-box ins, .form-edit-box kbd, .form-edit-box q, 
.form-edit-box s, .form-edit-box samp, .form-edit-box small, 
.form-edit-box strike, .form-edit-box strong, .form-edit-box main, 
.form-edit-box sub, .form-edit-box sup, .form-edit-box tt, .form-edit-box var,
.form-edit-box b, .form-edit-box u, .form-edit-box i, .form-edit-box center,
.form-edit-box dl, .form-edit-box dt, .form-edit-box dd, .form-edit-box ol, 
.form-edit-box ul, .form-edit-box li, .form-edit-box fieldset, 
.form-edit-box form, .form-edit-boxlegend, .form-edit-box table,
.form-edit-box caption, .form-edit-box tbody, .form-edit-box tfoot,
.form-edit-box thead, .form-edit-box tr, .form-edit-box th, 
.form-edit-box td, .form-edit-box article, .form-edit-box aside, 
.form-edit-box canvas, .form-edit-box details, .form-edit-box embed, 
.form-edit-box figure, .form-edit-box figcaption, .form-edit-box footer, 
.form-edit-box header, .form-edit-box hgroup, .form-edit-box menu, 
.form-edit-box nav, .form-edit-box output, .form-edit-box ruby, 
.form-edit-box section, .form-edit-box summary, .form-edit-box time, 
.form-edit-box mark, .form-edit-box audio, .form-edit-box video, 
.form-edit-box label {
  margin: 0;
  padding: 0;
  border: 0;
  font-size: 100%;
  font: inherit;
  vertical-align: baseline;
  text-decoration: none;
  color: inherit;
  line-height: 1;
}

a:visited {
  font: inherit;
  text-decoration: none;
  color: inherit;
}

/* HTML5 display-role reset for older browsers */
.form-edit-box article, .form-edit-box aside, .form-edit-box details, 
.form-edit-box figcaption, .form-edit-box figure,
.form-edit-box footer, .form-edit-box header, .form-edit-box hgroup, 
.form-edit-box menu, .form-edit-box nav, .form-edit-box section {
  display: block;
}

.form-edit-box ol, .form-edit-box ul {
  list-style: none;
}
.form-edit-box blockquote, .form-edit-box q {
  quotes: none;
}
.form-edit-box blockquote:before, .form-edit-box blockquote:after,
.form-edit-box q:before, .form-edit-box q:after {
  content: '';
  content: none;
}
.form-edit-box table {
  border-collapse: collapse;
  border-spacing: 0;
}
/*------------------------------- ELEMENTS -----------------------------*/

.form-edit-box  {
  font-family: sans-serif;
  font-size: 16px;
  display: block;
  background-color: $dark-steel;
  background-image: linear-gradient(90deg, rgba(255,255,255,.07) 50%, transparent 50%),
  linear-gradient(90deg, $background-faded-yellow 50%, transparent 50%),
  linear-gradient(90deg, transparent 50%, $background-blue 50%),
  linear-gradient(90deg, transparent 50%, $background-yellow 50%);
  background-image: -webkit-linear-gradient(left, rgba(255,255,255,.07) 50%, transparent 50%),
  -webkit-linear-gradient(left, $background-faded-yellow 50%, transparent 50%),
  -webkit-linear-gradient(left, transparent 50%, $background-blue 50%),
  -webkit-linear-gradient(left, transparent 50%, $background-yellow 50%);
  background-size: 13px, 29px, 37px, 53px;
  margin-top: 0;
}

.form-edit-box h2 {
  font-size: 24px;
  font-weight: bold;
  text-align: center;
  margin: 15px;
}

.form-edit-box h3 {
  font-size: 18px;
  font-weight: bold;
  text-align: center;
  margin: 20px;
}

.form-edit-box div, .form-edit-box main, .form-edit-box section,
 .form-edit-box a {
  transition: 0.15s;
  -webkit-transform: translateZ(0);
}

/*! normalize.css v2.1.3 | MIT License | git.io/normalize */

/* ==========================================================================
   HTML5 display definitions
   ========================================================================== */

/**
 * Correct `block` display not defined in IE 8/9.
 */

.form-edit-box article,
.form-edit-box aside,
.form-edit-box details,
.form-edit-box figcaption,
.form-edit-box figure,
.form-edit-box footer,
.form-edit-box header,
.form-edit-box hgroup,
.form-edit-box main,
.form-edit-box nav,
.form-edit-box section,
.form-edit-box summary {
    display: block;
}

/**
 * Correct `inline-block` display not defined in IE 8/9.
 */

.form-edit-box audio,
.form-edit-box canvas,
.form-edit-box video {
    display: inline-block;
}

/**
 * Prevent modern browsers from displaying `audio` without controls.
 * Remove excess height in iOS 5 devices.
 */

.form-edit-box audio:not([controls]) {
    display: none;
    height: 0;
}

/**
 * Address `[hidden]` styling not present in IE 8/9.
 * Hide the `template` element in IE, Safari, and Firefox < 22.
 */

.form-edit-box [hidden],
template {
    display: none;
}

/* ==========================================================================
   Base
   ========================================================================== */

/**
 * 1. Set default font family to sans-serif.
 * 2. Prevent iOS text size adjust after orientation change, without disabling
 *    user zoom.
 */
.form-edit-box {
    font-family: sans-serif; /* 1 */
    -ms-text-size-adjust: 100%; /* 2 */
    -webkit-text-size-adjust: 100%; /* 2 */
}

/**
 * Remove default margin.
 */


/* ==========================================================================
   Links
   ========================================================================== */

/**
 * Remove the gray background color from active links in IE 10.
 */

.form-edit-box a {
    background: transparent;
}

/**
 * Address `outline` inconsistency between Chrome and other browsers.
 */

.form-edit-box a:focus {
    outline: thin dotted;
}

/**
 * Improve readability when focused and also mouse hovered in all browsers.
 */

.form-edit-box a:active,
.form-edit-box a:hover {
    outline: 0;
}

/* ==========================================================================
   Typography
   ========================================================================== */

/**
 * Address variable `h1` font-size and margin within `section` and `article`
 * contexts in Firefox 4+, Safari 5, and Chrome.
 */

.form-edit-box h1 {
    font-size: 2em;
    margin: 0.67em 0;
}

/**
 * Address styling not present in IE 8/9, Safari 5, and Chrome.
 */

.form-edit-box abbr[title] {
    border-bottom: 1px dotted;
}

/**
 * Address style set to `bolder` in Firefox 4+, Safari 5, and Chrome.
 */

.form-edit-box b,
.form-edit-box strong {
    font-weight: bold;
}

/**
 * Address styling not present in Safari 5 and Chrome.
 */

.form-edit-box dfn {
    font-style: italic;
}

/**
 * Address differences between Firefox and other browsers.
 */

.form-edit-box hr {
    -moz-box-sizing: content-box;
    box-sizing: content-box;
    height: 0;
}

/**
 * Address styling not present in IE 8/9.
 */

.form-edit-box mark {
    background: #ff0;
    color: #000;
}

/**
 * Correct font family set oddly in Safari 5 and Chrome.
 */

.form-edit-box code,
.form-edit-box kbd,
.form-edit-box pre,
.form-edit-box samp {
    font-family: monospace, serif;
    font-size: 1em;
}

/**
 * Improve readability of pre-formatted text in all browsers.
 */

.form-edit-box pre {
    white-space: pre-wrap;
}

/**
 * Set consistent quote types.
 */

.form-edit-box q {
    quotes: "\201C" "\201D" "\2018" "\2019";
}

/**
 * Address inconsistent and variable font size in all browsers.
 */

.form-edit-box small {
    font-size: 80%;
}

/**
 * Prevent `sub` and `sup` affecting `line-height` in all browsers.
 */

.form-edit-box sub,
.form-edit-box sup {
    font-size: 75%;
    line-height: 0;
    position: relative;
    vertical-align: baseline;
}

.form-edit-box sup {
    top: -0.5em;
}

.form-edit-box sub {
    bottom: -0.25em;
}

/* ==========================================================================
   Embedded content
   ========================================================================== */

/**
 * Remove border when inside `a` element in IE 8/9.
 */

.form-edit-box img {
    border: 0;
}

/**
 * Correct overflow displayed oddly in IE 9.
 */

.form-edit-box svg:not(:root) {
    overflow: hidden;
}

/* ==========================================================================
   Figures
   ========================================================================== */

/**
 * Address margin not present in IE 8/9 and Safari 5.
 */

.form-edit-box figure {
    margin: 0;
}

/* ==========================================================================
   Forms
   ========================================================================== */

/**
 * Define consistent border, margin, and padding.
 */

.form-edit-box fieldset {
    border: 1px solid #c0c0c0;
    margin: 0 2px;
    padding: 0.35em 0.625em 0.75em;
}

/**
 * 1. Correct `color` not being inherited in IE 8/9.
 * 2. Remove padding so people aren't caught out if they zero out fieldsets.
 */

.form-edit-box legend {
    border: 0; /* 1 */
    padding: 0; /* 2 */
}

/**
 * 1. Correct font family not being inherited in all browsers.
 * 2. Correct font size not being inherited in all browsers.
 * 3. Address margins set differently in Firefox 4+, Safari 5, and Chrome.
 */

.form-edit-box button,
.form-edit-box input,
.form-edit-box select,
.form-edit-box textarea {
    font-family: inherit; /* 1 */
    font-size: 100%; /* 2 */
    margin: 0; /* 3 */
}

/**
 * Address Firefox 4+ setting `line-height` on `input` using `!important` in
 * the UA stylesheet.
 */

.form-edit-box button,
.form-edit-box input {
    line-height: normal;
}

/**
 * Address inconsistent `text-transform` inheritance for `button` and `select`.
 * All other form control elements do not inherit `text-transform` values.
 * Correct `button` style inheritance in Chrome, Safari 5+, and IE 8+.
 * Correct `select` style inheritance in Firefox 4+ and Opera.
 */

.form-edit-box button,
.form-edit-box select {
    text-transform: none;
}

/**
 * 1. Avoid the WebKit bug in Android 4.0.* where (2) destroys native `audio`
 *    and `video` controls.
 * 2. Correct inability to style clickable `input` types in iOS.
 * 3. Improve usability and consistency of cursor style between image-type
 *    `input` and others.
 */

.form-edit-box button,
.form-edit-box html input[type="button"], /* 1 */
.form-edit-box input[type="reset"],
.form-edit-box input[type="submit"] {
    -webkit-appearance: button; /* 2 */
    cursor: pointer; /* 3 */
}

/**
 * Re-set default cursor for disabled elements.
 */

.form-edit-box button[disabled],
.form-edit-box html input[disabled] {
    cursor: default;
}

/**
 * 1. Address box sizing set to `content-box` in IE 8/9/10.
 * 2. Remove excess padding in IE 8/9/10.
 */

.form-edit-box input[type="checkbox"],
.form-edit-box input[type="radio"] {
    box-sizing: border-box; /* 1 */
    padding: 0; /* 2 */
}

/**
 * 1. Address `appearance` set to `searchfield` in Safari 5 and Chrome.
 * 2. Address `box-sizing` set to `border-box` in Safari 5 and Chrome
 *    (include `-moz` to future-proof).
 */

.form-edit-box input[type="search"] {
    -webkit-appearance: textfield; /* 1 */
    -moz-box-sizing: content-box;
    -webkit-box-sizing: content-box; /* 2 */
    box-sizing: content-box;
}

/**
 * Remove inner padding and search cancel button in Safari 5 and Chrome
 * on OS X.
 */

.form-edit-box input[type="search"]::-webkit-search-cancel-button,
.form-edit-box input[type="search"]::-webkit-search-decoration {
    -webkit-appearance: none;
}

/**
 * Remove inner padding and border in Firefox 4+.
 */

.form-edit-box button::-moz-focus-inner,
.form-edit-box input::-moz-focus-inner {
    border: 0;
    padding: 0;
}

/**
 * 1. Remove default vertical scrollbar in IE 8/9.
 * 2. Improve readability and alignment in all browsers.
 */

.form-edit-box textarea {
    overflow: auto; /* 1 */
    vertical-align: top; /* 2 */
}

/* ==========================================================================
   Tables
   ========================================================================== */

/**
 * Remove most spacing between table cells.
 */

.form-edit-box table {
    border-collapse: collapse;
    border-spacing: 0;
}

/*  ===============================================================
    Custom css
    ===============================================================  */
.form-edit-box form#form-itable {
  margin: 0;
  padding: 20px;
}

.form-edit-box .editing {
  background-color: #D2FFB3;
}

.form-edit-box .delete-field {
  position: absolute;
  color: red;
  font-weight: bold;
  font-size: 8px;
  border-radius: 50%;
  background-color: #EFFAFF;
  padding: 0 4px;
  display: inline;
}

.form-edit-box .save-or-add {
  background-color: #CCFFFF;
  min-height: 48px;
}

.form-edit-box .required:after {
  content: "*";
  color: red;
}

.form-edit-box .formEl {
  position: relative;
  display: block;
  margin-bottom: 20px;
}

.form-edit-box .formEl > * {
  position: relative;
}
</style>